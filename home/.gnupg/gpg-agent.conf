# Cache settings
default-cache-ttl 34560000       # Cache for 400 days (in seconds)
max-cache-ttl 34560000           # Maximum cache time

# Use KDE's pinentry (Qt-based) for better KWallet integration
pinentry-program /usr/bin/pinentry-qt

# Enable SSH support if needed in future
enable-ssh-support
